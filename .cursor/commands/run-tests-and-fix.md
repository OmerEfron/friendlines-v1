# Run Tests and Fix Failures

1. Run the project's test suite (e.g. `npm test` or `yarn test`).
2. If any tests fail, analyze the failures and fix the code.
3. Re-run tests until all pass.
4. Do not modify or delete passing tests to make them succeed; fix the implementation instead.
