# Backend Tests

Placeholder. Add route/module existence and contract validity checks.

## Structure

- `routes/` — Route handler tests
- `modules/` — Module interface tests

## Pre-merge

Run `npm test` before merge. See project `/run-tests-and-fix` command.
